# Session 2026-06-05

## What was done

### 1. Emoji → Lucide React
- Navbar dark mode toggle: `☀️`/`🌙` → `<Sun />`/`<Moon />` (lucide-react)
- `package.json` & `package-lock.json` updated (lucide-react added)

### 2. Dark mode polish (full frontend audit)
Files touched: `Navbar.jsx`, `Dashboard.jsx`, `ProjectDetail.jsx`, `Login.jsx`, `Register.jsx`, `Modal.jsx`

| Component | Light | Dark |
|---|---|---|
| Navbar "Task Tracker" | `text-blue-600` | `dark:text-white` |
| Dashboard welcome + project name | default | `dark:text-white` |
| Dashboard desc, subtitle, empty state | `text-gray-500` | `dark:text-gray-300` |
| Filter tabs inactive | `text-gray-600` | `dark:text-gray-300 dark:hover:bg-gray-700` |
| All inputs/selects/textarea | `bg-white border-gray-300` | `dark:bg-gray-700 dark:text-gray-200 dark:border-gray-600` |
| Modal bg + title | `bg-white` | `dark:bg-gray-800 dark:text-gray-100` |
| Cancel buttons | `bg-gray-200` | `dark:bg-gray-600 dark:text-gray-200` |
| DroppableColumn (Kanban) | `bg-gray-50` | `dark:bg-gray-800/50` + `dark:border-x dark:border-b dark:border-gray-600` (full outline) |
| TaskCard | `bg-white` | `dark:bg-gray-700` |
| Column count badge | `bg-gray-200` | `dark:bg-gray-600` |
| Member list cards | `bg-gray-50` | `dark:bg-gray-700` |
| GitHub repo card | `bg-gray-50` | `dark:bg-gray-700` |
| Commit/comment cards | `bg-gray-50` | `dark:bg-gray-700/50` |
| Member/GitHub empty state | `text-gray-500` | `dark:text-gray-300` |

### 3. Removed status dropdown from TaskCard
- Status `<select>` di setiap task card dihapus karena bikin overflow (Delete kepotong)
- Drag-and-drop antar kolom tetap jadi cara utama ganti status

### 4. Fixed date timezone bug (due_date)
- **Root cause**: Kolom `due_date` di DB type `date`. PostgreSQL driver ngembaliin sebagai `Date` object di local timezone. JSON serialize jadinya UTC (`"2026-06-03T17:00:00.000Z"` untuk `2026-06-04` di UTC+7). Frontend pake `.split('T')[0]` ambil tanggal UTC, jadinya mundur 1 hari.
- **Fix**: `task.due_date.split('T')[0]` → `new Date(task.due_date).toLocaleDateString('en-CA')` (format YYYY-MM-DD di local timezone)

## Files modified
- `frontend/src/components/Navbar.jsx` — lucide icons, dark text
- `frontend/src/components/Modal.jsx` — dark bg/title
- `frontend/src/pages/Dashboard.jsx` — dark text/buttons
- `frontend/src/pages/ProjectDetail.jsx` — dark mode, remove dropdown, fix date
- `frontend/src/pages/Login.jsx` — dark inputs
- `frontend/src/pages/Register.jsx` — dark inputs
- `frontend/package.json` + `frontend/package-lock.json` — added lucide-react

## GitHub
- Issue created: https://github.com/LimeTeaX/task-tracker-api/issues/1
- Task created in app (Demo Project & user's Task Tracker project): "Dark mode polish & UI fixes"

## Known issues
- None
